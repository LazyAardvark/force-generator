import { RoleAuthDirective } from './role-auth.directive';

describe('RoleAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleAuthDirective();
    expect(directive).toBeTruthy();
  });
});
